[Service]
# -P: native pcap (decrypted inbound IKE) for protocol forensics
# -D 0x0001: DEBUG_FLAG_DEBUG (ikev2_config lease logs etc.)
ExecStart=
ExecStart=/usr/local/racoon2/sbin/iked -F -f /usr/local/racoon2/etc/racoon2/racoon2.conf -P /tmp/iked-native.pcap -D 0x0001
